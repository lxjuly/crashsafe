# 2026-09-08

## 2026-09-08T18:07:00Z Submission video refreshed

### Claims

- [settled] live-terminal-recording-refreshed: `crashsafe-demo.mov` is now a
  7.9-second cropped terminal recording of a fresh deterministic run.
- [settled] required-proof-visible: The recording visibly includes the durable
  charge before `kill -9`, SIGKILL exit, recovery under a new fence with the
  same operation key, attempts 1 and 2, workflow completion, a consistent
  event/projection audit, exactly one charge, and the final event timeline.
- [settled] recording-verified: Exact frames spanning the start, crash,
  recovery, and final proof were extracted from the committed movie and
  inspected before replacement.

### Commitments

- [accepted] focused-video-artifact: Keep the root recording short and scoped
  to the mandatory ambiguous-outcome guarantee; use `TESTING.md` and
  `make demo-features` for the broader feature walkthrough.

## 2026-09-08T19:05:00Z Materialized JSON workflow plan accepted

### Claims

- [settled] hardcoded-workflow-limits-demo: The current engine demonstrates
  durability well, but one Python-seeded linear workflow understates the
  reusability of its storage and worker model.
- [settled] parsing-is-not-the-focus: A registry, input-binding language,
  template resolver, or Temporal-style Python replay runtime would create a
  second problem unrelated to the take-home's core durability guarantee.
- [accepted] posted-plan-contract: `POST /workflows` will accept the complete
  materialized JSON plan: a name and concrete steps with IDs, allowlisted
  operations, dependencies, and operation-specific request bodies.
- [accepted] audit-is-internal: Event history remains durable authority and the
  reducer remains part of correctness verification, while public audit API and
  demo presentation will be removed.

### Commitments

- [accepted] no-definition-subsystem: Add no workflow registry, file loader,
  version resolver, discovery endpoint, stored definition CRUD, template,
  interpolation, output reference, or expression evaluator.
- [accepted] pydantic-and-graph-validation: Use a discriminated Pydantic union
  for typed tool requests, then validate unique step IDs, dependency targets,
  duplicate/self edges, and acyclicity.
- [accepted] immutable-history-snapshot: Generate operation keys from workflow
  ID plus client step ID and atomically persist the complete plan and dependency
  projections with `WorkflowCreated` before execution.
- [accepted] preserve-core-proof: Re-run every crash and retry test, retain
  internal reducer equality assertions, and refresh README, examples, demos,
  and video only after the same-key one-charge guarantee still passes.

### Plan projection

- `.memory/projections/plans/crashsafe-engine.md` now records accepted pending
  Phases 14–15 for the request contract, schema cutover, dependency-aware
  scheduler, public audit removal, tests, two example JSON payloads, README, and
  refreshed terminal evidence.

## 2026-09-08T19:18:00Z Phase 15 repository cleanup added

### Claims

- [settled] parallel-demo-harnesses-hurt-reviewability: The Temporal experiment,
  two Crashsafe demo scripts, and multiple Make targets widen the review surface
  without strengthening the core durability guarantee.
- [accepted] one-demo-surface: The final repository should contain one canonical
  `scripts/demo.py`, one uv invocation, and one root `crashsafe-demo.mov`.

### Commitments

- [accepted] remove-temporal-experiment: Delete `experiments/` and its Make and
  documentation references; keep Temporal only as optional architectural prose.
- [accepted] cohesive-demo-story: Consolidate the mandatory ambiguous charge,
  two submitted JSON workflows, concurrent workers, lease takeover, timelines,
  and final ledger into one deterministic demo with exactly one charge, two
  provisions, and three notifications.
- [accepted] tests-own-secondary-evidence: Prove adaptive backoff and graceful
  drain through focused tests and the concise manual instead of alternate demo
  modes or scripts.
- [accepted] final-file-inventory: Remove obsolete helpers, stale recordings,
  generated state, caches, and superseded docs during Phase 15, then verify the
  tracked file list before the final README and video refresh.

## 2026-09-08T22:05:00Z uv-only build requested

### Claims

- [settled] uv-available: uv 0.11.22 is installed and resolved the project in
  under a second.

### Commitments

- [accepted] remove-make: Delete the Makefile and make uv the sole documented
  dependency, test, lint, service, and demo interface.
- [accepted] lock-environment: Move development tools into the standard `dev`
  dependency group and commit the generated `uv.lock` for reproducibility.

## 2026-09-08T22:32:00Z Phases 14–15 implemented

### Claims

- [settled] direct-json-dag-live: The API validates complete materialized DAGs,
  stores them in `WorkflowCreated` v3, and schedules from normalized dependency
  rows with deterministic array-order tie breaking.
- [settled] invariant-preserved: Stable keys are now
  `{workflow_id}:{client_step_id}`; leases, fences, retry deadlines, tool-wide
  cooldowns, graceful drain, and tool idempotency retain their earlier meaning.
- [settled] one-demo-passed: `uv run python scripts/demo.py` passed with two
  workers and two workflows, fences `[1, 2]`, identical charge keys, exact
  projection reconstruction, and ledger counts `1/2/3`.
- [settled] focused-tests-expanded: DAG validation, reusable client step IDs,
  dependency readiness, deterministic branches, explicit legacy-schema refusal,
  and illegal-history dependency checks were added.
- [settled] repeated-uv-verification: All 38 tests passed three consecutive
  runs; Ruff and strict mypy passed, followed by another successful canonical
  crash demo.

### Commitments

- [accepted] recording-last-gate: Replace and inspect `crashsafe-demo.mov` only
  after the Mac is unlocked; the earlier capture was rejected because frame
  inspection showed the desktop instead of Terminal.

## 2026-09-08T22:39:00Z Phase 15 completed

### Claims

- [settled] video-refreshed: `crashsafe-demo.mov` is a 23.98-second, 1920×1080
  H.264 recording with one full-screen Terminal window. Extracted frames at five
  points show the uv command, real `kill -9`, `intent_recorded`, fences 1 and 2,
  same-key recovery, ledger `1/2/3`, both timelines, and the PASS line.
- [settled] phase-15-complete: The JSON DAG implementation, audit API removal,
  uv-only build, experiment deletion, sole demo, concise docs, repeated tests,
  and refreshed video now satisfy every Phase 14–15 acceptance item.

## 2026-09-09T01:45:00Z Focused manual scripts added

### Claims

- [settled] manual-scripts-verified: Executable curl-based scripts now exercise
  ambiguous-outcome `SIGKILL` recovery, HTTP 429 retry scheduling, and SIGTERM
  drain against a running local stack. Each prints JSON with `jq`, asserts its
  invariant, and passed against a fresh deterministic stack.
- [settled] testing-doc-consolidated: Focused two-terminal instructions and the
  complete verification command now live directly in `README.md`; the separate
  `TESTING.md` file was removed.

## 2026-09-09T03:15:00Z Workflow-run terminology implemented

### Claims

- [settled] definition-run-boundary: JSON files moved from `examples/` to
  `workflows/` and remain authoring inputs only. Each submitted definition creates
  an independently persisted execution with a server-generated `run_id`.
- [settled] public-run-api: The API now exposes `POST/GET /workflow_runs`, run
  detail, ordered events, and history-derived timeline endpoints; models and
  responses use `run_id` consistently.
- [settled] durable-schema-migration: SQLite schema v4 renamed workflow execution
  tables, foreign keys, and workflow-level event names. A startup v3-to-v4
  migration preserves nonempty databases, ordered history, projections, stable
  operation keys, and lease fences.
- [settled] terminology-verification: All 39 tests, Ruff, and strict mypy pass,
  including a migration reconstruction test. The canonical demo now reads its
  submitted definitions from `workflows/`.

### Commitments

- [accepted] precise-resource-model: Do not add workflow-definition CRUD or a
  registry. Treat a workflow as immutable submitted definition data and a
  workflow run as the durable executable resource.

## 2026-09-09T03:30:00Z Core demo story consolidated

### Claims

- [settled] ordered-two-run-demo: Worker 1 enters the ambiguous window after a
  durable charge. While it remains inside the tool call, worker 2 receives a
  deterministic operation-targeted 429 for the trial run and persists the
  two-second Retry-After; only then is worker 1 SIGKILLed.
- [settled] before-after-timelines: The demo prints both timelines immediately
  after SIGKILL, before recovery can race the snapshot, then starts one
  replacement worker and prints both completed timelines again.
- [settled] reduced-manual-surface: Crash resume, safe retry, adaptive backoff,
  concurrency, and observability are one coherent canonical demo. The separate
  crash and retry scripts were removed; graceful drain remains the sole focused
  manual script and always prints its timeline.
- [settled] combined-demo-verified: The live demo showed an unmatched charge
  attempt, a 2000 ms persisted retry, paid-run fences `[1, 2]`, same-key
  deduplication, exact projection reconstruction, final ledger `1/2/3`, and two
  completed timelines.

### Commitments

- [accepted] rerecord-current-demo: Replace the root video after the combined
  demo and workflow-run terminology are final; do not present the older capture
  as the current script output.

## 2026-09-09T03:32:00Z Graceful drain made self-contained

### Claims

- [settled] one-command-drain: `uv run python scripts/graceful_drain.py` now
  starts isolated API and tool services, creates a run, sends SIGTERM during the
  committed charge response, verifies the worker stays alive to commit only that
  attempt, starts a replacement, and verifies exact ledger counts.
- [settled] drain-timelines: The script prints the running timeline after the
  original worker exits and the completed timeline after restart using the same
  terminal-width formatter as the canonical demo.
- [settled] shell-helper-removed: `manual_helpers.sh` and the two-terminal
  `manual_graceful_drain.sh` were removed, eliminating curl, jq, and manual server
  startup from the README procedure.

### Commitments

- [accepted] two-script-surface: Keep only two executable scenarios:
  `scripts/demo.py` for the core crash/retry story and
  `scripts/graceful_drain.py` for the mutually exclusive SIGTERM story.

## 2026-09-09T03:45:00Z Graceful-drain startup hardened

### Claims

- [settled] opaque-timeout-cause: The first self-contained version hard-coded
  ports 8020/8021 and discarded service output, turning a child startup failure
  or port conflict into an unhelpful tool health-check timeout.
- [settled] dynamic-drain-ports: The script now asks the OS for two available
  loopback ports, prints the selected URLs, and includes captured process output
  if either service exits before becoming healthy.
- [settled] dynamic-drain-verified: The complete graceful-drain scenario passed
  on dynamically allocated ports with one charge attempt, exact ledger `1/1/1`,
  a running drained timeline, and a completed post-restart timeline.

## 2026-09-09T04:00:00Z Scenario databases made persistent

### Claims

- [settled] implicit-reset-removed: `scripts/demo.py` and
  `scripts/graceful_drain.py` no longer delete their dedicated state directories
  on startup; database files and completed run history persist until the user
  explicitly removes the scenario directory.
- [settled] repeatable-with-history: Both scripts snapshot the existing tool
  ledger and validate only the side-effect delta added by the current invocation.
  Each passed twice consecutively against the same growing databases.
- [settled] unfinished-state-protected: A prior unfinished scenario causes a
  clear error directing the user to recover or manually clear it, preventing old
  work from consuming deterministic failure hooks or being silently discarded.

## 2026-09-09T04:15:00Z Durability-domain filenames finalized

### Claims

- [settled] engine-database-name: `engine.db` is officially the workflow
  engine's database for immutable event history, definitions, projections,
  leases, fences, and retry scheduling.
- [settled] ledger-database-name: `ledger.db` is officially the mock service's
  independent database for durable idempotency results and side effects;
  `Settings.ledger_db` and `CRASHSAFE_LEDGER_DB` use the same terminology.
- [settled] tools-filename-migration: If `ledger.db` is absent but the former
  `tools.db` exists, `ToolStore` renames the database and any WAL/SHM sidecars
  before opening it. A focused test verifies the original receipt and one-charge
  ledger survive that migration.

## 2026-09-09T04:20:00Z Global state directory adopted

### Claims

- [settled] one-global-state-pair: The stack, canonical demo, graceful-drain
  check, and README walkthrough now default to the same `.crashsafe/engine.db`
  and `.crashsafe/ledger.db`; `CRASHSAFE_STATE_DIR` remains an explicit override.
- [settled] append-not-reset: Scenario startup removes only its stale signal file,
  never either database. Completed runs and ledger effects accumulate globally,
  and assertions use per-invocation deltas.

## 2026-09-09T04:35:00Z Architecture and critical paths documented

### Claims

- [settled] architecture-diagram: The README now traces client requests through
  FastAPI, the worker engine, `engine.db`, the mock tool, and `ledger.db`, and
  explicitly marks the at-least-once HTTP boundary between the two transactions.
- [settled] critical-path-comments: Concise invariant-focused comments cover run
  creation, SQLite durability, history/projection replay, runnable selection,
  claims and fencing, intent-before-I/O, retries, terminal commits, tool-side
  deduplication, graceful drain, and supervisor recovery.
- [settled] reviewability-verification: Ruff formatting/checks, strict mypy,
  `git diff --check`, and all 41 tests pass after the documentation-only pass.
