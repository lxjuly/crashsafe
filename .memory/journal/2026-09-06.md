# 2026-09-06

## 2026-09-06T17:08:00Z Durable engine implementation

### Claims

- [settled] assignment-reviewed: The full Stack AI Notion assignment was read before implementation.
- [settled] process-tests-pass: Unit, integration, static-type, lint, and real-process crash checks passed after implementation.
- [settled] python39-compatibility: An end-to-end run exposed Pydantic's runtime evaluation of Python 3.10 union syntax; annotations were corrected to retain Python 3.9 support.

### Commitments

- [accepted] safe-retries-feature: Safe retries with persisted backoff and numeric Retry-After support are the selected feature beyond mandatory crash-safe resume.
- [accepted] sqlite-full-sync: Apply SQLite WAL mode and synchronous=FULL on every connection involved in correctness-sensitive writes.
- [accepted] request-hash-validation: Reject reuse of an idempotency key when the operation or canonical request payload differs.

## 2026-09-06T17:19:00Z Memory scope correction

### Claims

- [settled] memory-request-intent: The requested project-memory addition is lightweight documentation based on Chronelle, not an application feature.

### Commitments

- [accepted] remove-memory-service: Remove the initially over-scoped memory API, SQLite store, packaging entry point, stack process, and CRUD tests.
- [accepted] git-is-memory-ledger: Maintain current state in `.memory/MEMORY.md` and detailed context in dated journal entries.

## 2026-09-06T17:40:00Z Graceful drain

### Claims

- [settled] graceful-drain-verified: SIGTERM during an in-flight delayed charge allows the charge response and completion transaction to finish, prevents provision from starting, and exits the worker with code 0.
- [settled] graceful-restart-proof: Restart completes provision and notification while the durable tool ledger remains at exactly one charge.

### Commitments

- [accepted] finish-current-attempt: Graceful drain finishes the current synchronous tool attempt rather than attempting unsafe mid-request cancellation.
- [accepted] interruptible-idle-wait: Use an event-backed poll wait so idle workers react promptly to shutdown signals.
- [accepted] document-grace-period: Require deployment termination grace to cover the configured request timeout; SIGKILL remains the fallback durability path.

### Projection

- `.memory/projections/plans/graceful-drain.md` records the retrospective implementation plan, outcome, evidence, and limits.

## 2026-09-06T17:45:00Z Full implementation plan projection

### Claims

- [settled] earlier-plan-too-narrow: The first requested implementation-plan projection covered only graceful drain rather than the entire take-home.
- [settled] full-plan-added: `.memory/projections/plans/crashsafe-engine.md` now records the complete engine objective, scope, architecture, data model, state machine, transaction boundaries, phased implementation, verification matrix, corrections, and production follow-ups.

### Commitments

- [accepted] full-plan-is-primary: Use the Crashsafe engine projection as the primary retrospective implementation plan and retain the graceful-drain projection as supporting detail.

## 2026-09-06T18:08:39Z Event-history plan revision

### Claims

- [settled] distribution-is-separate: Shards, history trees, branches, leases, and fencing solve scale or concurrent ownership rather than the single-worker durability requirement.
- [settled] retry-policy-is-separate: Retry configuration determines an eligible time; durability requires persisting the resulting absolute `next_attempt_at`, not any particular Retry-After policy.
- [settled] honest-attempt-fact: The engine can durably record an attempt before network I/O but cannot atomically prove a request was sent, so an unmatched `StepAttemptStarted` represents an unknown outcome.

### Commitments

- [accepted] append-only-log: Add immutable, strictly ordered workflow events as the authoritative logical history.
- [accepted] rebuildable-projections: Keep workflow and step rows for efficient scheduling, update them atomically with events, and verify them against a pure event reducer.
- [accepted] preserve-idempotency-boundary: Continue to describe one external side effect as a property of the mock tool's atomic idempotency ledger, not of the engine history alone.
- [accepted] phase-nine-plan: Implement schema and typed events, reducer, atomic transition writer, inspection/audit, then failure and reconstruction tests as Phase 9 of the primary plan.

### Projection

- `.memory/projections/plans/crashsafe-engine.md` now focuses its next phase and acceptance criteria on how event history satisfies crash durability without importing Temporal's distributed storage machinery.

## 2026-09-06T18:55:07Z Final phases reshuffled

### Claims

- [settled] write-up-follows-proof: The final README and video should describe a completed and verified event-history implementation, not precede it.
- [settled] readme-format: The final write-up will be succinctly organized as problem statement, functional requirements, non-functional requirements, architecture, core data model and event-history invariants, state machine, and demo.

### Commitments

- [accepted] event-history-is-phase-eight: Implement and verify the append-only history, reducer, atomic projections, and crash reconstruction in Phase 8.
- [accepted] submission-is-phase-nine: Make README finalization, a followable deterministic demo, and terminal video recording the last phase.
- [accepted] demo-checkpoints: The demo will visibly show the committed charge, missing first-attempt completion, hard worker kill, same-key recovery attempt, completed workflow, consistent folded projection, and exactly one ledger charge.

### Projection

- `.memory/projections/plans/crashsafe-engine.md` records the revised ordering, concise README outline, video expectation, and six concrete demo steps.

## 2026-09-06T19:06:48Z Event history and README completed

### Claims

- [settled] event-history-implemented: Workflow creation, attempt start, retry scheduling, step completion/failure, and workflow completion/failure now append typed events atomically with their projections.
- [settled] reconstruction-verified: A pure reducer validates event order and transitions, reproduces the stored projection, detects corruption, and supports explicit projection rebuild.
- [settled] transaction-kill-verified: A real child process killed after an uncommitted `StepCompleted` insert but before projection update exposes neither half after restart.
- [settled] final-checks-pass: `make test` reports 18 passing tests, `make lint` passes Ruff and strict mypy, and `make demo` passes all four visible checkpoints.
- [settled] readme-finished: `README.md` now contains the succinct final submission write-up and recording steps.

### Commitments

- [accepted] history-api: Retain read-only `/workflows/{id}/events` and `/workflows/{id}/audit` endpoints for inspectable durability evidence.
- [accepted] legacy-database-safety: Refuse nonempty pre-history databases rather than synthesizing events that never occurred; document `make clean` for disposable development state.
- [accepted] record-video: Preserve the completed four-checkpoint recording at `docs/crashsafe-demo.mov`.

### Projection

- `.memory/projections/plans/crashsafe-engine.md` records Phases 8 and 9, including the README, demo, and video, as complete.

## 2026-09-07T03:08:48Z One-worker Temporal comparison

### Claims

- [settled] same-guarantee-observed: After the mock tool committed a charge and the only Temporal worker was killed, a replacement worker ran Activity attempt 2 with the identical operation key and received `deduplicated=True`.
- [settled] temporal-ledger-proof: The Temporal workflow completed with `{charges: 1, provisions: 1, notifications: 1}` in the existing durable mock-tool ledger.
- [settled] retry-history-shape: The observed Temporal history recorded the successful charge as Activity attempt 2 with `activity StartToClose timeout` as its previous failure rather than preserving the first start as a separate completed history event.

### Commitments

- [accepted] isolated-temporal-example: Keep the comparison in `experiments/temporal_compare/` and run its SDK through `uv` on Python 3.12 without changing Crashsafe's Python 3.9 runtime.
- [accepted] same-external-boundary: Use the exact Crashsafe mock tool and stable workflow/position/operation key so the comparison tests the orchestration engines rather than different side-effect behavior.

### Evidence

- `make demo-temporal` passed against Temporal CLI 1.8.2, Server 1.31.2, and Temporal Python SDK 1.18.2.
- The original 18-test suite, Ruff, and strict mypy still pass.

## 2026-09-07T23:20:00Z Submission video

### Claims

- [settled] recorded-live-recovery: `docs/crashsafe-demo.mov` is a 23.99-second, 1476x986 recording of the real ambiguous-charge process flow.
- [settled] recorded-proof: The video shows the original and replacement worker PIDs, two persisted charge attempts with one operation key, an audit-consistent projection, and `{charges: 1, provisions: 1, notifications: 1}`.

### Commitments

- [accepted] assertion-gated-recording: The browser recording harness raises an error instead of printing `PASS` unless it sees repeated requests with one key and exactly one charge.
- [accepted] honest-guarantee-walkthrough: The final frame states durable recovery plus at-least-once requests and attributes exactly-once effects to the external tool's idempotency contract.
